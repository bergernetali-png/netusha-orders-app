# פרומפט מוכן: בניית אפליקציית ניהול הזמנות דומה לזו של NETUSHA

זה טקסט שאפשר להעתיק בדיוק כמו שהוא ולשלוח לחברה שלך — היא מדביקה אותו כהודעה ראשונה ל-Claude (או כל כלי AI לתכנות אחר), והוא יבנה לה אפליקציה עצמאית משלה, בלי שום קשר לנתונים או לחשבונות שלך.

לפני ששולחים, כדאי שהיא תמלא בעצמה את הסוגריים המסומנים `[...]` לפי העסק שלה.

---

## הפרומפט לשליחה

אני רוצה אפליקציית ווב פשוטה לניהול הזמנות עבור העסק שלי — `[סוג העסק, למשל: מאפייה בוטיק / סטודיו לתכשיטים / קייטרינג קטן]`.

**ארכיטקטורה (חשוב לשמור על זה, בלי לסטות):**
- Node.js טהור (גרסה 18 ומעלה), CommonJS, בלי שום framework (לא Express, לא React) ובלי dependencies חיצוניות.
- שרת HTTP פשוט עם המודול המובנה `http`.
- כל התוכן נשמר כקבצי JSON בתיקיית `data/` — בלי מסד נתונים.
- התחברות עם סיסמת מנהל/ת יחידה ממשתנה סביבה (`.env`), session מבוסס עוגייה חתומה.
- HTML נבנה בצד שרת (כל מסך = פונקציית JS שמחזירה מחרוזת HTML) — בעברית מלאה, RTL, מותאם מובייל קודם כל.
- CSS עם custom properties לצבעי המותג שלי: `[פלטת הצבעים של העסק שלי]`.
- `.gitignore` שמוציא את `data/*.json` ואת `.env` — כדי שנתוני לקוחות וסודות לא יעלו ל-GitHub.

**מבנה תיקיות:** `server.js` (ניתוב), `lib/` (עזרים משותפים — חישובים, אימות, פורמט), `views/` (מסכים), `public/css` + `public/js` (עיצוב וקוד לקוח), `data/*.json` (הזמנות והגדרות).

**פיצ'רים:**
1. **דשבורד**: כרטיסי סטטיסטיקה (הזמנות פתוחות, הכנסות החודש, רווח החודש, אחוז רווח ממוצע), ורשימת "הזמנות קרובות" שמראה שם לקוח/ה, מה הוזמן, מחיר, סטטוס תשלום ותאריך — עם אפשרות לעדכן את הסטטוס ישירות מהרשימה בלי לפתוח את ההזמנה.
2. **רשימת הזמנות**: חיפוש לפי שם לקוח/ה, סינון לפי סטטוס, ייצוא ל-CSV, מחיקה, ועדכון סטטוס מהיר בכל שורה.
3. **טופס הזמנה**: פרטי לקוח/ה, כמה פריטים בהזמנה אחת (אפשרות להוסיף/להסיר שורות, עד תקרה סבירה), ולכל פריט: כמות, מחיר מכירה, עלות, ו-`[שדות נוספים ספציפיים לעסק שלי, למשל: עלות חומרי גלם / עלות הדפסה / זמן ייצור]`. חישוב רווח חי תוך כדי הקלדה, בשתי שורות: "רווח מוצר" (לפני זמן עבודה) ו"רווח נטו" (אחרי קיזוז עלות זמן העבודה לפי תעריף שעתי מההגדרות). מעקב תשלום: לא שולם / מקדמה / שולם במלואו, וסכום ששולם בפועל.
4. **לוח שנה חודשי**: רשת חודש רגילה (שבוע בכל שורה), ובכל יום — שמות הלקוחות שאמורים לקבל אספקה באותו יום, צבועים לפי סטטוס ההזמנה. לחיצה על שם פותחת את ההזמנה שלו; לחיצה על התאריך פותחת רשימה מלאה של כל ההזמנות לאותו יום.
5. **הגדרות**: שם העסק, תעריף שעתי לחישוב עלות זמן עבודה, ו-`[הגדרות נוספות ספציפיות אם צריך]`.
6. **מובייל**: תפריט תחתון קבוע (bottom nav) עם כפתור "הזמנה חדשה" בולט במרכז, ו-PWA בסיסי (אפשר להוסיף לבית) בלי service worker (כדי שלא יהיה סיכון של מידע לא מעודכן).

**חשוב לשמור עליו:**
- בלי React/Vue/Next — רק פונקציות JS שמחזירות HTML כמחרוזת.
- קל לאירוח על כל שרת Node רגיל (למשל Railway) בלי שלב build.
- RTL תקין בכל מקום, וכל טופס/כפתור נוח גם למגע באצבע.

**סטטוסים רלוונטיים לעסק שלי:** `[לדוגמה: התקבלה / בהכנה / מוכנה למסירה / נמסרה / בוטלה — או כל תהליך אחר שמתאים]`.

---

## מה כדאי שהיא תסגור לפני שהיא שולחת

- סוג העסק והמוצרים (מה בדיוק מוכרים).
- שדות העלות שרלוונטיים לפריט (לא כל עסק צריך "עלות מיתוג" כמוני — אולי אצלה זה "עלות חומר גלם" או "עלות אריזה").
- פלטת הצבעים של המותג שלה.
- אם יש לה גם ספקים שצריך לעקוב אחריהם (כמו העמוד "ספקים וקניות" אצלי) — אם כן, כדאי להוסיף שורה על זה בפרומפט.
- הסטטוסים שמתאימים לתהליך העבודה שלה.

## חשוב לגבי אחסון ופרסום

זו צריכה להיות אפליקציה נפרדת לגמרי משלי — repo חדש משלה ב-GitHub, פרויקט חדש משלה ב-Railway (או שירות דומה), סיסמה וסודות משלה. אסור לחבר את זה לחשבון שלי או להשתמש באותם פרטי התחברות.
